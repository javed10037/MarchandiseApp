'use strict';

var Error = require('../error');

module.exports = function (merchant, middlware) {
  if (!merchant.merchantId || !merchantId.merchantSecret) throw new Error('Merchant ID or Merchant Secret are missing');

  function IPN(merchant) {
    this.merchant = merchant;
    this.middlware = this.defaultFn.bind(this);
  }

  IPN.prototype.register = function (fn) {};

  IPN.prototype.getPrivateHeadersIPN = function () {};

  IPN.prototype.defaultFn = function (req, res, next) {};
};